{
  "name": "projectgt-web",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix react_app && npm run build --prefix react_app",
    "start": "cd react_app && npx next start -H 0.0.0.0 -p ${PORT:-8080}"
  }
}
