# Чек-лист приведения модуля Cash Flow в соответствие со стандартами

## 📱 Соответствие @.cursor/rules/flutter.mdc

### 🧱 Архитектура и State Management
- [x] Рефакторинг `CashFlowState` в `lib/presentation/state/cash_flow_state.dart`: перевести на `@freezed` (сейчас используется обычный класс с ручным `copyWith`).

### 🧩 Design System (UI/UX)
- [x] **Экран списка** (`lib/features/cash_flow/presentation/screens/cash_flow_list_screen.dart`):
    - [x] Заменить стандартный `Scaffold` на `EdgeToEdgeScaffold`.
- [x] **Форма транзакции** (`lib/features/cash_flow/presentation/widgets/cash_flow_form_dialog.dart`):
    - [x] Заменить все `TextFormField` на `GTTextField`.
    - [x] Заменить все `DropdownButtonFormField` на `GTDropdown`, `GTEnumDropdown` или `GTStringDropdown`.
    - [x] Проверить возможность замены `SegmentedButton` на компоненты из Design System (если применимо).

### 🪜 Форматирование данных
- [x] **Виджеты** (`lib/features/cash_flow/presentation/widgets/cash_flow_form_dialog.dart` и др.):
    - [x] Удалить прямые импорты `package:intl/intl.dart` (кроме случаев в Data-слое, если необходимо).
    - [x] Заменить `DateFormat('dd.MM.yyyy').format(...)` на `formatRuDate(...)` из `lib/core/utils/formatters.dart`.

### 📝 Документирование и стиль
- [x] Добавить отсутствующие Doc-comments (`///`) для публичных методов в `CashFlowNotifier` и моделях.
- [x] Проверить наличие неиспользуемых импортов и переменных.

---

## 📚 Соответствие @.cursor/rules/documentation.mdc

### 📖 Модульная документация
- [x] Провести аудит и актуализировать `docs/cash_flow/cash_flow_module.md`:
    - [x] Обновить раздел **Presentation** после замены виджетов на компоненты Design System.
    - [x] Обновить раздел **State Management** после перевода на `Freezed`.
    - [x] Добавить (если отсутствуют) подразделы: **Интеграции**, **Roadmap**, **ASCII-дерево файлов**.
    - [x] Проверить актуальность описания таблиц и RLS-политик.

