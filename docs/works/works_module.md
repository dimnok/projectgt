# Модуль Works (Shifts & Work Plans)
**Дата актуализации:** 10 января 2026 года (Проведен рефакторинг UI с использованием системных виджетов проекта: GTButtons, GTTextField, EdgeToEdgeScaffold и др.)

## Важное замечание о структуре данных
> **Внимание:**
> - Модуль полностью переведен на модель **Multi-tenancy**. Таблицы `works`, `work_items`, `work_materials`, `work_hours`, `work_plans`, `work_plan_blocks`, `work_plan_items` изолированы через `company_id`.
> - Все операции выполняются через Supabase (REST, Realtime, Storage). RLS-политики завязаны на членство в компании (`public.get_my_company_ids()`).
> - Критические зависимости: справочники `objects`, `employees`, `estimates`, `profiles`, Supabase Storage bucket `works`, Edge Function `send_admin_work_event`.

## Детальное описание модуля
Модуль **Works** объединяет функционал ежедневных смен (Shifts) и долгосрочного планирования (Work Plans).

1.  **Смены (Works/Shifts):** Обеспечивают учёт фактически выполненных работ, материалов и трудозатрат по факту.
2.  **Планы работ (Work Plans):** Позволяют формировать детальный план по системам и участкам на будущие периоды.

**Ключевые функции:**
- ✅ Загрузка списка смен и планов с фильтрацией по компании и объектам.
- ✅ Детальный просмотр смены/плана с разбивкой по работам.
- ✅ Фотофиксация для смен (утренние/вечерние фото).
- ✅ Многоуровневое планирование: План → Блоки (Системы/Участки) → Работы.
- ✅ **UI Рефакторинг (10.01.2026):** Все экраны и виджеты модуля переведены на использование Design System проекта (`lib/core/widgets/`).

**Архитектурные особенности:**
- Clean Architecture: `data → domain → presentation`.
- Вся бизнес-логика изолирована по `company_id` (Multi-tenancy).
- **Presentation:** Использование `EdgeToEdgeScaffold` для поддержки прозрачных панелей, `GTButtons` для единообразия действий, `GTTextField` и `GTDropdown` для ввода данных.
- **Всплывающие окна:** Использование `ModalContainerWrapper` для модальных окон и `GTConfirmationDialog` для подтверждения действий.
- **Уведомления:** Использование `AppSnackBar` для системных сообщений.

## Используемые таблицы и зависимости
**Основные таблицы модуля:** 
- `works`, `work_items`, `work_materials`, `work_hours` (Смены)
- `work_plans`, `work_plan_blocks`, `work_plan_items` (Планы)

**Связанные таблицы из других модулей:**
- `objects`, `profiles` — контекст, владелец, объект.
- `employees` — сотрудники в сменах и ответственные в планах.
- `estimates` — сметные позиции.

## Структура и файлы модуля
**Presentation/UI**
- `lib/features/works/` — UI для ежедневных смен.
- `lib/features/work_plans/` — UI для планирования работ.
  - `screens/work_plans_list_screen.dart` — список планов.
  - `screens/work_plan_details_screen.dart` — детальный вид плана (desktop).
  - `screens/work_plan_edit_screen.dart` — полноэкранное редактирование.
  - `screens/work_plan_form_content.dart` — содержимое формы.
  - `screens/work_plan_form_modal.dart` — контейнер формы.
  - `widgets/work_plans_mobile_cards.dart` — карточки списка (mobile).
  - `widgets/work_plan_mobile_details.dart` — детальный вид (mobile).
  - `widgets/work_block_widget.dart` — виджет блока работ.
  - `widgets/work_selection_widget.dart` — выбор работ из сметы.

**Domain**
- `lib/domain/entities/work.dart`, `work_item.dart`, `work_hour.dart`, `work_material.dart`
- `lib/domain/entities/work_plan.dart` — сущность плана, блоков и элементов.

**Data**
- `lib/data/models/work_model.dart`, `work_plan_model.dart` — модели с аннотациями `@freezed` и `@JsonSerializable`.
- `lib/data/datasources/work_data_source_impl.dart`, `work_plan_data_source.dart`.
- `lib/data/repositories/work_repository_impl.dart`, `work_plan_repository_impl.dart`.

## Дерево структуры модуля (Work Plans)
```
lib/features/work_plans/
├── presentation/
│   ├── screens/
│   │   ├── work_plans_list_screen.dart
│   │   ├── work_plan_details_screen.dart
│   │   ├── work_plan_edit_screen.dart
│   │   ├── work_plan_form_content.dart
│   │   └── work_plan_form_modal.dart
│   └── widgets/
│       ├── work_block_state.dart
│       ├── work_block_widget.dart
│       ├── work_plan_mobile_details.dart
│       ├── work_plans_mobile_cards.dart
│       └── work_selection_widget.dart
```

## База данных и RLS-политики

### Таблицы Планирования

#### 1. `work_plans` (Заголовок плана)
| Колонка | Тип | NULL | Описание |
|---------|-----|------|----------|
| id | uuid | NO | PK |
| company_id | uuid | NO | FK → `companies.id` |
| date | date | NO | Дата плана |
| object_id | uuid | NO | FK → `objects.id` |
| created_by | uuid | NO | FK → `profiles.id` |

#### 2. `work_plan_blocks` (Группировка)
| Колонка | Тип | NULL | Описание |
|---------|-----|------|----------|
| id | uuid | NO | PK |
| work_plan_id | uuid | NO | FK → `work_plans.id` (CASCADE) |
| system | text | NO | Система (электрика и т.д.) |
| section | text | YES | Участок |
| floor | text | YES | Этаж |
| responsible_id | uuid | YES | FK → `employees.id` |
| worker_ids | uuid[] | NO | Массив исполнителей |
| company_id | uuid | NO | FK → `companies.id` |

#### 3. `work_plan_items` (Работы в плане)
| Колонка | Тип | NULL | Описание |
|---------|-----|------|----------|
| id | uuid | NO | PK |
| block_id | uuid | NO | FK → `work_plan_blocks.id` (CASCADE) |
| estimate_id | uuid | YES | FK → `estimates.id` |
| name | text | NO | Название работы |
| unit | text | NO | Ед. изм. |
| price | numeric | NO | Цена из сметы |
| planned_quantity| numeric | NO | Запланированный объем |
| actual_quantity | numeric | NO | Фактически выполненный объем |
| company_id | uuid | NO | FK → `companies.id` |

**RLS:** ✅ Включён для всех таблиц. Доступ ограничен через `company_id` (функция `get_my_company_ids()`).

## Бизнес-логика и ключевые особенности

### Планирование (Work Plans)
- **Иерархия:** План создается на конкретную дату и объект. Внутри плана может быть несколько блоков. Каждый блок объединяет работы по одной системе/участку и имеет своего ответственного.
- **Дизайн:** Использование кастомных виджетов из Design System обеспечивает строгость и минимализм интерфейса. Кнопки действий (Primary/Secondary/Text) четко разделяют приоритеты операций.
- **Валидация:** Поля ввода (`GTTextField`) и выпадающие списки (`GTDropdown`) поддерживают встроенную валидацию. Кнопка сохранения блокируется при неполном заполнении.

## Текущие ограничения и планы развития
- ✅ **Реализовано:** Полный CRUD для планов, Multi-tenancy изоляция, рефакторинг UI под Design System.
- 🔴 **Проблема:** Требуется синхронизация фактических объемов из Смен (`works`) в Планы (`work_plans`) для автоматического расчета `actual_quantity`.
- 🔄 **Планы:** Реализовать триггер или Edge Function для связки `work_items` (факт) и `work_plan_items` (план) по `estimate_id` и дате.

## Примечания для разработчиков
- Всегда используйте виджеты из `lib/core/widgets/` вместо стандартных Material-компонентов.
- При вызове диалогов и снекбаров используйте `GTConfirmationDialog.show` и `AppSnackBar.show`.
- Следите за `company_id` при создании вложенных структур.
