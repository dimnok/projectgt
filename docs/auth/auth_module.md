# Модуль Auth (Аутентификация и Доступ)
**Дата:** 11 января 2026 г.

Обновление документации согласно финальной схеме входа: переход на Phone-only Auth, интеграция OTP в LoginScreen и оптимизация UX (отсутствие промежуточных лоадеров).

## 📋 Важное замечание
Модуль является критическим узлом системы. Он управляет не только входом, но и контекстом Multi-tenancy (выбор компании). Все данные в БД защищены RLS, завязанным на `auth.uid()` и членство в компаниях.

## 📖 Описание
Обеспечивает безопасный вход пользователей по номеру телефона через Telegram или SMS. 
Ключевые функции:
- Авторизация по номеру телефона (Phone-only).
- Интеграция с Telegram Mini App (для Web).
- Управление состояниями: Onboarding (создание компании), Pending Approval (ожидание доступа), Active.
- Multi-tenancy: переключение между рабочими пространствами.

## 🔗 Зависимости
### Таблицы модуля (Owner)
- `public.profiles`: профили пользователей (phone, email, status, last_company_id).
- `public.company_members`: связь пользователей с компаниями и их роли.

### Таблицы других модулей (Usage)
- `public.companies`: данные организаций (используется для контекста при входе).

## 🎨 Слой Presentation
### Экраны
- `LoginScreen`: единая точка входа. Поддерживает два шага (ввод телефона и ввод OTP) в одном интерфейсе с анимациями.
- `AuthGate`: центральный контроллер навигации. Переключает экраны на основе `AuthStatus` без лишних перерисовок.
- `OnboardingScreen`: экран выбора/создания компании для новых пользователей.

### Оптимизация UX (No Intermediate Loaders)
В финальной схеме реализован бесшовный переход:
1. При верификации OTP `AuthNotifier` обновляет данные пользователя в фоне, не меняя глобальный статус `loading` в `AuthGate`.
2. `LoginScreen` показывает локальный индикатор верификации.
3. Переход на главный экран (или onboarding) происходит мгновенно после подгрузки профиля через `AnimatedSwitcher` в `AuthGate`.

## ⚙️ Слой Domain/Data
### Сущности
- `User`: базовая модель аутентификации (id, email, phone, roleId, systemRole).
- `AuthState`: состояние с перечислением `AuthStatus`.

### Состояние (Riverpod)
- `authProvider` (`AuthNotifier`): логика входа, выхода, смены компании и проверки сессии.
- `currentUserProfileProvider`: подгружает детальные данные профиля после успешного входа.

### Репозитории
- `AuthRepository`: методы `requestPhoneOtp`, `verifyPhoneOtp`, `logout`.

## 📂 Дерево файлов
```text
lib/features/auth/
├── domain/
│   ├── entities/
│   │   └── user.dart
│   ├── repositories/
│   │   └── auth_repository.dart
│   └── usecases/
│       ├── request_phone_otp_usecase.dart
│       └── verify_phone_otp_usecase.dart
├── data/
│   ├── datasources/
│   │   └── auth_remote_data_source.dart
│   └── repositories/
│       └── auth_repository_impl.dart
└── presentation/
    ├── screens/
    │   ├── login_screen.dart
    │   ├── onboarding_screen.dart
    │   └── pending_approval_screen.dart
    └── widgets/
        └── telegram_login_button.dart
```

## 🗄️ База данных (Audit)
### Таблицы
- **profiles**: `id` (uuid, PK), `phone` (text, unique), `email` (text), `status` (bool), `last_company_id` (uuid).
- **company_members**: `user_id` (uuid), `company_id` (uuid), `system_role` (text).

### RLS (Row-Level Security)
- ✅ **profiles**: 
  - `SELECT`: только свой профиль или профили коллег по компании.
  - `UPDATE`: только свой профиль.
- ✅ **company_members**:
  - `SELECT`: только записи своей компании.

## ⚡ Интеграции
- **Edge Functions**:
  - `otp-notisend`: отправка кода в Telegram/SMS.
  - `telegram-auth`: быстрая авторизация для Telegram Mini App.
- **Внешние пакеты**:
  - `supabase_flutter`: основная инфраструктура.
  - `flutter_animate`: плавные переходы в `LoginScreen` и `AuthGate`.

## 🗺️ Roadmap
- [x] Переход на Phone-only Auth.
- [x] Интеграция OTP в основной экран входа.
- [x] Удаление промежуточных лоадеров в `AuthGate`.
- [ ] 🟢 Реализация автоматического подхвата кода (Auto-fill) на мобильных устройствах.
- [ ] 🟡 Оптимизация времени холодного старта Edge Functions.
