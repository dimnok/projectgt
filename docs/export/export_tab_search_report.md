# 📊 Модуль "Поиск по работам"

**Дата обновления:** 15 января 2025 года (убрана функциональность выгрузки)

---

## 🎯 Обзор

Экран "Поиск по работам" (`ExportScreen` → `ExportTabSearch`) — расширенный поиск и фильтрация работ по наименованию материалов с поддержкой фильтров по объекту и периоду дат.

---

## 📁 Структура файлов

### Presentation Layer (UI)
- **`export_screen.dart`** (135 строк)
  - Основной экран модуля поиска
  - Управление состоянием поиска
  - Отображение AppBar и результатов

- **`export_tab_search.dart`** (1250 строк)
  - Основной виджет таба поиска
  - Управление состоянием поиска
  - Отображение результатов в PlutoGrid

### Providers (State Management)
- **`work_search_provider.dart`** (190 строк)
  - `WorkSearchState` — состояние поиска
  - `WorkSearchNotifier` — управление поиском
  - `workSearchProvider` — Riverpod провайдер

- **`work_search_date_provider.dart`** (7 строк)
  - `workSearchDateRangeProvider` — период дат для поиска

### Domain Layer
- **`work_search_repository.dart`** (22 строки)
  - Абстрактный репозиторий поиска

- **`work_search_result.dart`** (86 строк)
  - Freezed entity результата поиска

### Data Layer
- **`work_search_repository_impl.dart`** (32 строки)
  - Реализация репозитория

- **`work_search_data_source.dart`** (22 строки)
  - Абстрактный data source

- **`work_search_data_source_impl.dart`** (115 строк)
  - Реализация через Supabase

### Виджеты UI
- **`export_search_action.dart`** (189 строк)
  - Поле поиска в AppBar с debounce

- **`work_search_date_filter.dart`** (417 строк)
  - Календарь для выбора периода дат

- **`work_search_export_action.dart`** (164 строки)
  - Кнопка экспорта результатов в Excel

- **`export_search_filter_chips.dart`** (554 строки)
  - Чипы для фильтрации по системам, участкам, этажам

- **`export_work_item_edit_modal.dart`** (655 строк)
  - Модальное окно для редактирования работы

### Сервисы
- **`work_search_export_server_service.dart`** (120 строк)
  - Сервис экспорта результатов поиска в Excel через Edge Function

### Провайдеры репозиториев
- **`repositories_providers.dart`** (15 строк)
  - Провайдеры для работы с Supabase

---

## 🔍 Функциональность

### Поиск
- **Debounce:** 500ms задержка после ввода
- **Поиск по:** наименованию работ (`work_items.name`)
- **Фильтры:**
  - Период дат (`workSearchDateRangeProvider`)
  - Объект (выпадающий список)
  - Система, участок, этаж (динамические фильтры)
  - Поисковый запрос (текстовое поле)

### Результаты поиска
- **Таблица:** PlutoGrid с колонками
- **Пагинация:** 250 элементов на странице
- **Итоговая строка:** автоматический подсчёт

### Состояния UI
- **Загрузка:** `CupertinoActivityIndicator`
- **Ошибка:** карточка с иконкой и текстом ошибки
- **Пусто:** сообщение "Введите запрос для поиска"
- **Результаты:** таблица PlutoGrid

---

## 🏗️ Архитектура

### Clean Architecture
```
Presentation → Domain ← Data
     ↓            ↓        ↓
  Provider   Repository  Supabase
```

### Поток данных
1. **Ввод запроса** → `_searchController`
2. **Debounce** → `_performSearch()` (500ms)
3. **Поиск** → `workSearchProvider.notifier.searchMaterials()`
4. **Репозиторий** → `WorkSearchRepository.searchMaterials()`
5. **Data Source** → `WorkSearchDataSourceImpl.searchMaterials()`
6. **Supabase** → запросы к `work_items` и `works`
7. **Результаты** → `WorkSearchResult` → UI

---

## 🗄️ Работа с БД

### Запросы Supabase

**Шаг 1:** Поиск `work_items` по имени
```dart
client.from('work_items')
  .select('*')
  .ilike('name', '%$searchQuery%')
```

**Шаг 2:** Фильтрация `works` по датам и объекту
```dart
client.from('works')
  .select('*, objects(name)')
  .inFilter('id', workIds)
  .gte('date', startDate)  // если указано
  .lte('date', endDate)    // если указано
  .eq('object_id', objectId) // если указано
```

**Шаг 3:** Объединение результатов
- Сопоставление `work_items` с отфильтрованными `works`
- Формирование `WorkSearchResult`

### Таблицы БД
- **`work_items`** — работы/материалы
- **`works`** — смены (связь с объектами)
- **`objects`** — объекты (join через `works`)

---

## 🎨 UI/UX Особенности

### Адаптивность
- **Desktop:** полнофункциональный интерфейс с таблицей
- **Mobile/Tablet:** упрощённый интерфейс или сообщение о недоступности
- **ResponsiveUtils:** проверка `isDesktop(context)`

### Дизайн
- **Карточка фильтров:** скруглённые углы, тени, адаптивный цвет фона
- **Таблица:** тёмная/светлая тема, кастомные стили PlutoGrid
- **Итоговая строка:** выделение жирным шрифтом и синим цветом

### Интерактивность
- **Автопоиск:** debounce 500ms
- **Динамические фильтры:** обновляются при выборе системы
- **Редактирование:** модальное окно для изменения данных

---

## ⚙️ Технические детали

### Состояние поиска
```dart
class WorkSearchState {
  final List<WorkSearchResult> results;
  final bool isLoading;
  final String? error;
  final int currentPage;
  final int pageSize;
  final int totalCount;
}
```

### Entity результата
```dart
@freezed
class WorkSearchResult {
  final DateTime workDate;
  final String objectName;
  final String objectId;
  final String system;
  final String subsystem;
  final String section;
  final String floor;
  final String workName;
  final String materialName;
  final String unit;
  final num quantity;
}
```

### Провайдеры Riverpod
- `workSearchProvider` — состояние поиска
- `workSearchDateRangeProvider` — период дат
- `exportSearchQueryProvider` — поисковый запрос
- `exportSelectedObjectIdProvider` — выбранный объект
- `exportSearchFilterProvider` — фильтры по системам, участкам, этажам

---

## 🔗 Интеграции

### С другими модулями
- **Объекты:** использование `objectProvider` из общих провайдеров
- **Период дат:** независимый `workSearchDateRangeProvider`
- **Работы:** связь с модулем работ через `work_items` и `works`

### Зависимости
- `pluto_grid` — таблица результатов
- `intl` — форматирование дат
- `supabase_flutter` — запросы к БД
- `flutter_riverpod` — управление состоянием
- `excel` — экспорт в Excel
- `file_saver` — сохранение файлов

---

## ⚠️ Особенности реализации

### 1. Двойной поиск
- Сначала поиск по `work_items.name`
- Затем фильтрация по `works` (даты, объект)
- Объединение результатов в памяти

### 2. Debounce
- Таймер отменяется при каждом изменении
- Поиск выполняется только если запрос не пустой

### 3. Динамические фильтры
- Фильтры обновляются в зависимости от выбранной системы
- Система → участки → этажи (каскадная фильтрация)

### 4. Экспорт результатов
- Экспорт через Edge Function на Supabase
- Формат: Excel с форматированием
- Включает все результаты поиска

---

## 📊 Статистика кода

- **Всего файлов:** 17
- **Строк кода:** ~4,500
- **Слои:**
  - Presentation: ~2,600 строк
  - Domain: 108 строк
  - Data: 169 строк
  - Providers: 200 строк
  - Services: 120 строк

---

## ✅ Выводы

### Сильные стороны
- ✅ Чистая архитектура (Clean Architecture)
- ✅ Debounce для оптимизации запросов
- ✅ Адаптивный UI (desktop/mobile)
- ✅ Подробная обработка состояний (loading, error, empty)
- ✅ Динамические фильтры с каскадной логикой
- ✅ Экспорт результатов в Excel
- ✅ Редактирование данных из результатов поиска

### Рекомендации
- 🔄 Рассмотреть оптимизацию запросов (индексы в БД)
- 🔄 Добавить кэширование результатов поиска
- 🔄 Сохранение последних поисковых запросов
- 🔄 Экспортная функция для избранных результатов

---

**Последнее обновление:** 15 января 2025 года
