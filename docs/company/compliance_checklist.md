# Чек-лист приведения модуля Company в соответствие с правилами проекта

Этот чек-лист содержит пункты, которые необходимо выполнить для приведения модуля `lib/features/company/` в соответствие с требованиями `.cursor/rules/flutter.mdc`.

## 🎨 UI и Design System
- [x] **Замена стандартных полей ввода**:
    - В файле `lib/features/company/presentation/widgets/company_bank_account_edit_dialog.dart` заменить `TextFormField` на `GTTextField`.
    - В файле `lib/features/company/presentation/widgets/company_document_edit_dialog.dart` заменить `TextFormField` на `GTTextField`.
- [x] **Соблюдение стилистики**: Проверить отступы и использование `const` во всех новых виджетах.

## 🛠 Форматирование данных
- [x] **Использование единых форматтеров**:
    - В файле `lib/features/company/presentation/screens/company_screen.dart` заменить прямое использование `DateFormat` на `formatRuDate` из `lib/core/utils/formatters.dart`.
    - В файле `lib/features/company/presentation/widgets/company_document_edit_dialog.dart` заменить `DateFormat` на `formatRuDate`.
- [x] **Очистка импортов**: Удалить `import 'package:intl/intl.dart';` из всех файлов в слое `presentation`, заменив его на импорт `lib/core/utils/formatters.dart`.

## 🧱 Архитектура
- [x] **Data Layer**: Перенести логику поиска компании по ИНН (если она есть) в `CompanyDataSource` (сейчас реализовано, но проверить соответствие).
- [x] **Domain Layer**: Убедиться, что все Freezed-классы объявлены как `abstract class`.

## 📚 Документация
- [x] **Создание файла модуля**: Создать `docs/company/company_module.md` согласно "Стандарту документации модулей".
- [x] **Описание БД**: Добавить в документацию структуру таблиц `companies`, `company_bank_accounts`, `company_documents` и описание RLS политик.

---
*Дата создания: 4 января 2026 г.*

