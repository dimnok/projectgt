# Модуль Компания (Company)
*Дата последнего обновления: 8 января 2026 г.*

## 📌 Важное замечание
Модуль является центральным узлом для Multi-tenancy архитектуры. Он управляет организациями, их реквизитами, банковскими счетами и документами. Каждая компания имеет владельца (Owner) и участников (Members) с определёнными ролями.

## 📝 Описание
Модуль предназначен для:
- Создания и настройки профиля организации.
- Управления банковскими реквизитами (поддержка нескольких счетов, выбор основного).
- Хранения лицензий, СРО и других нормативных документов.
- Реализации механизма приглашений через `invitation_code`.
- Связки пользователей с организациями.

## 🔗 Зависимости
- **Таблицы модуля (Owner):** `companies`, `company_bank_accounts`, `company_documents`, `company_members`.
- **Использование (Usage):** `profiles` (last_company_id), `roles`.

## 🎨 Слой Presentation
- **Экраны:**
    - `CompanyScreen`: Основной экран с детальной информацией, реквизитами и документами.
    - `CompanyOnboardingScreen`: Экран выбора/создания/вступления в компанию при первом входе.
- **Виджеты:**
    - `CompanyInfoCard`, `CompanyInfoRow`: Компоненты отображения данных.
    - `CompanyFormContent`: Общий контент для форм создания и редактирования.
    - `CompanyProfileEditDialog`, `CompanyBankAccountEditDialog`, `CompanyDocumentEditDialog`, `CompanyCreateDialog`.
- **Providers:**
    - `companyProfileProvider`: FutureProvider профиля текущей компании.
    - `companyBankAccountsProvider`: Список счетов.
    - `companyDocumentsProvider`: Список документов.
    - `activeCompanyIdProvider`: ID активной компании из профиля пользователя.
    - `userCompaniesProvider`: Список всех компаний, в которых состоит пользователь.

## ⚙️ Слой Domain/Data
- **Сущности (Entities):** `CompanyProfile`, `CompanyBankAccount`, `CompanyDocument` (все реализованы через Freezed).
- **Use Cases:** `CreateCompanyUseCase`, `JoinCompanyUseCase`.
- **Repositories:** `CompanyRepository`, `CompanyRepositoryImpl`.
- **Data Sources:** `CompanyDataSource`, `SupabaseCompanyDataSource`.
- **State Management (Core):** `currentUserProfileProvider` — используется для отслеживания `last_company_id` и автоматической навигации. Поддерживает принудительное обновление (`refreshCurrentUserProfile`) для мгновенной синхронизации после создания компании.

## 🌳 Дерево файлов
```text
lib/features/company/
├── data/
│   ├── datasources/
│   │   └── company_data_source.dart
│   ├── models/
│   └── repositories/
│       └── company_repository_impl.dart
├── domain/
│   ├── entities/
│   │   ├── company_bank_account.dart
│   │   ├── company_document.dart
│   │   └── company_profile.dart
│   ├── repositories/
│   │   └── company_repository.dart
│   └── usecases/
│       ├── create_company_usecase.dart
│       └── join_company_usecase.dart
└── presentation/
    ├── providers/
    │   └── company_providers.dart
    ├── screens/
    │   ├── company_onboarding_screen.dart
    │   └── company_screen.dart
    └── widgets/
        ├── company_bank_account_edit_dialog.dart
        ├── company_create_dialog.dart
        ├── company_document_edit_dialog.dart
        ├── company_form_content.dart
        ├── company_info_widgets.dart
        └── company_profile_edit_dialog.dart
```

## 🗄️ База данных (Audit)

### Таблицы

#### `companies` (Организации)
- `id`: uuid (Primary Key)
- `name_full`: text (Полное наименование)
- `name_short`: text (Краткое наименование)
- `owner_id`: uuid (FK -> profiles.id)
- `inn`, `kpp`, `ogrn`, `okpo`: text (Реквизиты)
- `legal_address`, `actual_address`: text (Адреса)
- `director_name`, `director_position`, `director_basis`: text (Руководство)
- `invitation_code`: text (Уникальный код приглашения)
- `is_active`: boolean (Статус активности)
- **RLS:** ✅ Включён.
    - `SELECT`: Для участников компании (`get_my_company_ids()`) и для владельцев (`owner_id = auth.uid()`).
    - `INSERT`: Разрешено для всех авторизованных пользователей (для создания своей компании).
    - `UPDATE`: Только для `owner_id`.

#### `company_bank_accounts` (Банковские счета)
- `id`: uuid (Primary Key)
- `company_id`: uuid (FK -> companies.id)
- `bank_name`, `bank_city`, `account_number`, `corr_account`, `bik`: text
- `is_primary`: boolean (Флаг основного счета)
- **RLS:** ✅ Включён. Доступ для участников соответствующих компаний.

#### `company_documents` (Документы)
- `id`: uuid (Primary Key)
- `company_id`: uuid (FK -> companies.id)
- `type`, `title`, `number`: text
- `issue_date`, `expiry_date`: date
- `file_url`: text
- **RLS:** ✅ Включён. Доступ для участников соответствующих компаний.

#### `company_members` (Участники)
- `company_id`, `user_id`: uuid (Composite Key)
- `system_role`: text (owner, admin) — системный статус
- `role_id`: uuid (FK -> roles.id) — кастомная роль
- `is_owner`: boolean (Legacy)
- **RLS:** ✅ Включён.
    - `SELECT`: Участники видят других участников той же компании.
    - `INSERT`: Разрешено для авторизованных пользователей (при создании компании — авто-назначение owner).
    - `UPDATE`: Только для участников с `system_role = 'owner'`.

## 🛠 Интеграции
- **DaData API:** Поиск реквизитов по ИНН через Edge Function `dadata-proxy`.
- **Supabase Auth & RLS:** Обеспечение изоляции данных между разными организациями.

## 🚀 Roadmap
- [x] Создание организации.
- [x] Вступление по коду.
- [x] Управление реквизитами и документами.
- [ ] 🟡 Смена владельца компании.
- [ ] 🟢 Логирование изменений профиля.

