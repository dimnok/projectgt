# Система аутентификации и профилей

> ⚠️ **Внимание:** Этот файл содержит общую информацию. Актуальная техническая документация модуля находится в [docs/auth/auth_module.md](auth/auth_module.md).

## Обзор
Система переведена на **Phone-only Auth** (вход по номеру телефона). Email больше не используется как первичный способ входа, но сохраняется в метаданных профиля.

### Основные изменения (Январь 2026):
1. **Phone-only**: Вход через Telegram или SMS.
2. **In-place OTP**: Ввод кода подтверждения интегрирован прямо в экран логина.
3. **Smooth UX**: Удалены промежуточные лоадеры при переходе от верификации к приложению.
4. **Multi-tenancy**: Доступ к данным изолирован внутри компаний.

Подробности реализации, структура БД и RLS описаны в основном файле модуля:
👉 **[Читать auth_module.md](auth/auth_module.md)**
