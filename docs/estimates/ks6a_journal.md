# Журнал КС-6а (Периоды выполнения)

**Дата актуализации:** 25 января 2026 года
**Статус:** Реализовано (Управление периодами в заголовке, удаление ручного ввода)

---

## 📖 Описание
Журнал КС-6а в системе — это инструмент для финансового закрытия работ по договору за определенные периоды. Данные в КС-6а формируются автоматически на основе ежедневного журнала работ (`work_items`) и фиксируются как юридически значимые акты. 

**Ключевые особенности:**
- **Автоматизация:** Все данные подтягиваются из подтвержденных ежедневных отчетов.
- **Отсутствие ручного ввода:** Для обеспечения точности и аудита, ручная корректировка объемов в КС-6а отключена. Все правки должны вноситься в исходные ежедневные отчеты.
- **Пиксельная точность:** Синхронизация высоты строк и заголовков между фиксированной левой частью и скроллируемым телом таблицы.
- **Единый стиль:** Цвета, границы и шрифты полностью синхронизированы с основным модулем смет.

---

## 🛠 Структура данных

### 1. Периоды (`ks6a_periods`)
Заголовочная таблица, определяющая временной диапазон и статус актирования.
- **company_id:** Ссылка на компанию (Multi-tenancy).
- **contract_id:** Ссылка на договор.
- **Статусы:**
  - `draft` (Черновик) — данные доступны для обновления из отчетов и удаления.
  - `approved` (Согласовано) — данные заморожены, любые изменения запрещены на уровне БД (RLS).
- **Связи:** Один договор (`contracts`) может иметь множество периодов. Периоды не должны перекрываться (рекомендуется программная проверка).

### 2. Строки периода (`ks6a_period_items`)
Спецификация объемов по каждой сметной позиции.
- **company_id:** Ссылка на компанию (Multi-tenancy).
- **period_id:** Ссылка на заголовок периода.
- **estimate_id:** Ссылка на сметную позицию.
- **quantity:** Объем работ за период (рассчитывается из отчетов).
- **price_snapshot:** Фиксированная цена из сметы на момент создания периода.
- **amount:** Расчетная сумма (`quantity * price_snapshot`).

---

## 🔄 Бизнес-процесс

### Шаг 1: Инициация периода
Пользователь выбирает диапазон дат (например, с 10.01 по 05.02). Система вызывает RPC `initialize_ks6a_period`:
1. Создает запись в `ks6a_periods` со статусом `draft`.
2. Собирает все `work_items` по договору за этот диапазон.
3. Группирует их по `estimate_id` и создает записи в `ks6a_period_items`.

### Шаг 2: Актуализация черновика
В интерфейсе журнала КС-6а пользователь видит сформированные объемы.
- Если были изменены ежедневные отчеты в `work_items`, пользователь нажимает кнопку **«Обновить данные»**, которая вызывает RPC `refresh_ks6a_period`. Это полностью пересчитывает объемы из отчетов.

### Шаг 3: Утверждение
При переводе в статус `approved` (через RPC `approve_ks6a_period`):
1. Период становится доступным только для чтения.
2. Данные этого периода фиксируются для всех последующих отчетов.

---

## 🛠 API (RPC функции)

### 1. `initialize_ks6a_period`
Создает новый черновик периода и автоматически заполняет его данными из ежедневных отчетов.
- **Аргументы:** `company_id`, `contract_id`, `start_date`, `end_date`, `title`.
- **Возвращает:** `UUID` созданного периода.

### 2. `refresh_ks6a_period`
Полностью пересчитывает объемы в черновике на основе актуальных данных из `work_items`.
- **Аргументы:** `period_id`.

### 3. `approve_ks6a_period`
Переводит период в статус `approved`, фиксирует итоговую сумму и блокирует дальнейшие изменения.
- **Аргументы:** `period_id`.

### 4. `get_contract_ks6a_data`
Возвращает полную структуру данных для отображения Журнала КС-6а: список всех периодов договора и их строки.
- **Аргументы:** `company_id`, `contract_id`.
- **Возвращает:** `JSONB { periods: [...], items: [...] }`.

---

## 🔐 Безопасность (RLS)
- **Multi-tenancy:** Все таблицы модуля содержат `company_id`. Доступ фильтруется напрямую: `company_id IN (public.get_my_company_ids())`.
- **Защита от изменений:** Политики RLS для `UPDATE` и `DELETE` жестко проверяют `status = 'draft'`. Как только статус меняется на `approved`, записи становятся иммутабельными.

---

## 🔌 Интеграция с UI
- **ViewModel:** `Ks6aRowViewModel` из `lib/features/estimates/presentation/utils/ks6a_processor.dart` преобразует данные БД в плоский список.
- **Виджет:** `Ks6aTableView` отображает периоды как статические колонки данных.
- **Управление:** Кнопки **«Сформировать период»**, **«Обновить»** и **«Согласовать»** вынесены в заголовок правой панели экрана `estimate_desktop_view.dart`. Кнопки «Обновить» и «Согласовать» отображаются динамически только при наличии активного черновика (`status = 'draft'`).
- **Стилизация:** Используется `TableBorder` для консистентного отображения сетки, аналогично `EstimateTableView`. Цвета заголовков адаптируются под светлую и темную темы.
