# Модуль Сметы (Estimates)

**Дата последнего обновления:** 18.01.2026
**Список изменений:**
- Проведен полный рефакторинг `EstimateEditDialog` и `ImportEstimateFormModal`: переход на кастомные виджеты Design System (`GTTextField`, `GTDropdown`, `GTStringDropdown`).
- Полностью удалены "рукописные" обертки над `TypeAheadField` (файл `dropdown_typeahead_field.dart` удален), все выпадающие списки теперь соответствуют единому дизайн-коду.
- Модуль полностью переведен на использование проектных форматтеров (`GtFormatters`). Реализован парсинг ввода через `parseAmount` и отображение процентов через `formatPercentage`.
- Оптимизировано управление: удалена колонка действий из таблицы, введено единое контекстное меню по правому клику для всех режимов.
- Бизнес-логика расчета номеров позиций перенесена из UI в `EstimateNotifier`.
- Улучшена система обновления данных: реализована асинхронная инвалидация провайдеров через `EstimateActionsMixin`.

## 📌 Описание модуля
Модуль предназначен для управления сметными позициями объектов строительства. Позволяет импортировать данные из Excel, вести учет выполнения работ и привязывать материалы из накладных к конкретным позициям сметы.

## 🛠 Зависимости
### Таблицы модуля (owner):
- `estimates` — основная таблица сметных позиций.

### Таблицы других модулей (usage):
- `objects` — привязка к строительным объектам.
- `contracts` — связь с договорами.
- `work_items` — данные о выполнении (через связь с `works`).
- `materials` — складские остатки и накладные.

## 🎨 Слой Presentation
### Экраны:
- `EstimatesListScreen` — список всех сметных файлов, сгруппированных по объектам и договорам.
- `EstimateDetailsScreen` — детальный просмотр позиций конкретной сметы.
  - `EstimateDesktopView` — десктопная версия с боковой панелью навигации и таблицей.
  - `EstimateMobileView` — мобильная версия со списком карточек.
- `ImportEstimateFormModal` — форма импорта новых смет из Excel.

### Виджеты (Design System):
- `GTTextField` — используется для всех текстовых и числовых полей ввода.
- `GTDropdown` / `GTStringDropdown` — для выбора объектов, договоров, систем, подсистем и единиц измерения.
- `EstimateTableView` — кастомная таблица (на базе `Table`) с поддержкой контекстного меню.
- `GTPrimaryButton`, `GTSecondaryButton` — основные действия.

### Форматирование:
- Весь вывод данных (валюта, даты, количества, проценты) и парсинг ввода реализованы через единые форматтеры проекта (`lib/core/utils/formatters.dart`). 
- **Парсинг:** используется `parseAmount` (обрабатывает пробелы, запятые и точки).
- **Проценты:** используется `formatPercentage` (единообразный вывод с символом % и русской локалью).
- Ручные методы форматирования и парсинга (через `double.tryParse` с заменой запятых) полностью удалены из модуля.

### Состояние (Riverpod):
- `estimateNotifierProvider` — управление CRUD-операциями.
- `estimateGroupsProvider` — сгруппированный список смет.
- `estimateItemsProvider` — позиции конкретной сметы.
- `estimateCompletionByIdsProvider` — данные о фактическом выполнении.

## 🏗 Слой Domain/Data
- **Сущность:** `Estimate` (`lib/domain/entities/estimate.dart`).
- **Модель:** `EstimateModel` (`lib/data/models/estimate_model.dart`).
- **Репозиторий:** `EstimateRepositoryImpl`.
- **Use Cases:** 
  - `GetEstimatesUseCase`, `CreateEstimateUseCase`, `UpdateEstimateUseCase`, `DeleteEstimateUseCase`.

## 📂 Дерево файлов
```
lib/features/estimates/
├── data/
│   ├── datasources/
│   │   └── estimate_data_source.dart
│   ├── models/
│   │   ├── estimate_model.dart
│   │   └── estimate_completion_model.dart
│   └── repositories/
│       └── estimate_repository_impl.dart
├── domain/
│   ├── entities/
│   │   └── estimate.dart
│   └── repositories/
│       └── estimate_repository.dart
└── presentation/
    ├── mixins/
    │   └── estimate_actions_mixin.dart
    ├── providers/
    │   └── estimate_providers.dart
    ├── screens/
    │   ├── estimate_desktop_view.dart
    │   ├── estimate_details_screen.dart
    │   ├── estimate_mobile_view.dart
    │   ├── estimates_list_screen.dart
    │   └── import_estimate_form_modal.dart
    └── widgets/
        ├── estimate_edit_dialog.dart
        ├── estimate_table_view.dart
        ├── estimate_item_card.dart
        ├── estimate_item_details_dialog.dart
        └── material_from_receipts_picker.dart
```

## 🗄 База данных (Audit)
### Таблица `estimates`:
- **Columns:**
  - `id`: `uuid` (Primary Key, default: `gen_random_uuid()`)
  - `company_id`: `uuid` (NOT NULL)
  - `system`: `text` (NOT NULL)
  - `subsystem`: `text` (NOT NULL)
  - `number`: `text` (NOT NULL) — порядковый номер (поддерживает формат "д-1").
  - `name`: `text` (NOT NULL)
  - `article`: `text` (NOT NULL)
  - `manufacturer`: `text` (NOT NULL)
  - `unit`: `text` (NOT NULL)
  - `quantity`: `double precision` (NOT NULL)
  - `price`: `double precision` (NOT NULL)
  - `total`: `double precision` (NOT NULL)
  - `estimate_title`: `text`
  - `object_id`, `contract_id`: `uuid`
  - `created_at`, `updated_at`: `timestamptz`

- **RLS:** ✅ Включён.
  - `Users can view data of their companies`: SELECT (через `get_my_company_ids()`).
  - `Users can manage data of their companies`: ALL (INSERT, UPDATE, DELETE).

### Интеграции с Edge Functions:
- `excel_parse` — парсинг загруженных файлов смет.
- `xls_to_xlsx` — конвертация старых форматов Excel.
- `generate_vor` — генерация ведомости объемов работ.

## ⚙️ Бизнес-логика
1. **Группировка:** Сметы не имеют отдельной таблицы "Заголовков". Группировка происходит динамически по полю `estimate_title` + `object_id` + `contract_id`.
2. **Нумерация:** Автоматический инкремент номера происходит в `EstimateNotifier.calculateNextNumber`. Поддерживается префикс "д-" для дополнительных работ.
3. **Обновление:** При редактировании позиции вызывается `_invalidateCurrentList` в миксине, что сбрасывает кэш провайдеров `estimateItemsProvider` и `estimateGroupsProvider`.

## 🗺 Roadmap
- 🟢 Переход на кастомные виджеты (Design System) — **DONE**
- 🟢 Контекстное меню для всех строк — **DONE**
- 🟡 Массовое редактирование позиций (в планах)
- 🔴 Исправление бага скролла при большом количестве позиций (высокий приоритет)
