# Контрагенты

**Дата:** 02.09.2026  
**Экран:** `/contractors` (только десктоп)  
**Данные:** таблица `contractors`, та же база, что у рабочего приложения

Справочник заказчиков, подрядчиков и поставщиков компании.

## Что умеет

- Список слева. Справа: фильтр по типу, кнопка добавления, сводка по всем карточкам компании
- Поиск: на широком экране сверху, на узком — под сводкой. Ищет по имени, ИНН, директору, телефону, email, коду и названию ОКВЭД
- Карточка по клику: полное имя, сфера деятельности (название и код ОКВЭД), коды, адреса, люди, контакты, банковские счета. Краткое имя в шапке не дублируется. Пустые поля скрыты. Кнопки «Изменить» и «Удалить» контрагента расположены сверху карточки. Телефон, почта и сайт — ссылки. Телефон: `+7 900 000 00 00`
- Банковские счета: выделены в отдельный блок внутри открытой карточки с бейджем количества счетов, кнопкой «+ Добавить счёт» и карточками каждого счёта. Реквизиты (БИК, Р/С, К/С) расположены в единую строку для удобного чтения и копирования. Поля формы как в приложении: банк, БИК (9 цифр), номер счёта и корр. счёт (по 20 цифр), переключатель «Счёт по умолчанию». Город банка показывается, если уже есть в базе, в форме его нет. Основным может быть только один счёт
- Создание, изменение, удаление
- Заполнение по ИНН: поле ИНН стоит первым в форме, справа кнопка «Поиск по ИНН». Можно нажать Enter в поле. Подставляются названия, КПП, ОГРН, ОКПО, адреса, директор, код ОКВЭД, телефон и почта, если они есть в ответе. Пустые поля из ответа не затирают уже введённое

Изменения сразу пишутся в рабочую базу.

Пустое поле в форме сохраняется в базу как `null`, не как пустая строка. Во Flutter с 02.09.2026 такой `null` читается как пустой текст, иначе падал весь список контрагентов.

## Правила

- Видны только контрагенты активной компании
- Система налогообложения выбирается из списка, не вводится руками: ОСНО, УСН «Доходы», УСН «Доходы минус расходы», АУСН (оба варианта), ЕСХН, ПСН, НПД или «Не указана». НДС — отдельный переключатель и ставка
- Сфера деятельности: в форме вводится только код ОКВЭД. Название подставляется из справочника, руками его писать не нужно. В базу пишется код (`43.29`). На карточке: `название (код)`. Если код неизвестен — показывается сам код. Старые записи вида `[код] название` или `название (код)` читаются
- Поиск по ИНН идёт через ту же серверную функцию `dadata-proxy`, что и в приложении. Ключ DaData в браузер не попадает. Юридический адрес копируется в фактический, как в приложении
- В одной компании нельзя сохранить двух контрагентов с одним ИНН. Форма покажет имя уже существующего. В базе тоже стоит запрет, в том числе для приложения
- Тип по умолчанию — заказчик. Если в базе тип пустой или неизвестный — тоже заказчик
- Сводка считает все карточки компании, даже если список отфильтрован
- Если у контрагента есть договоры, удаление может не пройти — так устроено в базе

## ОКВЭД

Справочник: ОКВЭД 2 (ОК 029-2014), изменение №89 от 01.08.2026. Файл: `src/lib/okved/okved2.json` (выгрузка classifikators.ru от 10.08.2026).

При заполнении по ИНН из ответа берётся только код. Название по-прежнему из этого справочника, не из текста DaData.

В мобильном приложении справочника пока нет: там видно сырое значение из базы.

## Типы

| Тип в базе | На экране | Цвет |
| --- | --- | --- |
| `customer` | Заказчик | тёмный |
| `contractor` | Подрядчик | зелёный |
| `supplier` | Поставщик | жёлтый |

Тип виден на карточке. У открытой карточки рамка и подкрашенный заголовок того же цвета.

## Пока нет

Это есть в приложении, в вебе пока нет:

- логотип
- мобильный экран

## Файлы

```
src/app/contractors/page.tsx
src/features/contractors/
  api/          запросы к базе, поиск по ИНН, счета
  hooks/        загрузка и сохранение
  types/        типы карточки, формы и счетов
  utils/        типы, налог, фильтр, сводка, подстановка по ИНН, счета
  ui/desktop/   список, фильтры, сводка
  ui/shared/    карточка, форма, удаление, счета
src/lib/okved/  справочник ОКВЭД и подстановка названия
```

Права: `src/config/permissions.ts` → `contractors`.  
Пункт меню: `src/config/navigation.ts`.

Приложение: [docs/contractors/contractors_module.md](../../docs/contractors/contractors_module.md).
