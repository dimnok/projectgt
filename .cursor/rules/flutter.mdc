---
description: Правила для Flutter-приложения с использованием Supabase и актуализацией знаний через context7
globs: lib/**/*.dart, test/**/*.dart
alwaysApply: true
---

# 📱 Общие требования

- **Целевые платформы**: iOS, Android, macOS, Windows
- **Фреймворк**: Flutter
- **Бэкенд**: Supabase
- **Стиль**: строгий черно-белый минимализм
- **Темы**: поддержка светлой и тёмной темы
- **Удаляй неиспользуемые переменные, функции и виджеты, которые появляются в ходе рефакторинга или доработки кода.**
- **Документирование кода (Dart Doc-Comments)**:
  - Используй полные и четкие doc-комментарии (`///`) для всех публичных API: классов, функций, методов и глобальных алиасов.
  - Комментарии должны объяснять **назначение** элемента, описывать параметры и возвращаемые значения.
  - Обязательно снабжай документацией глобальные алиасы (например, в `formatters.dart`) для обеспечения качественных подсказок в IDE.

---

## 🧱 Архитектура и структура проекта

- Придерживайся **Clean Architecture** с разделением на слои:
  - `presentation/` — UI
  - `domain/` — бизнес-логика
  - `data/` — работа с API/БД
- Организуй код по фичам: `lib/features/<feature_name>/`
- Используй `Riverpod` для управления состоянием и внедрения зависимостей
- Применяй `Freezed` для создания неизменяемых и sealed-классов
- **Всегда используй `abstract class` при объявлении Freezed-классов** (для поддержки mixins и корректной генерации).
- Используй `json_serializable` для сериализации/десериализации моделей

---

## 🎨 UI и UX

- Используй `go_router` для маршрутизации
- Создавай **адаптивные** и **переиспользуемые** виджеты
- Всегда используй `const`-конструкторы, где это возможно
- Применяй `LayoutBuilder`, `MediaQuery`, `ThemeData` для адаптивности
- Учитывай доступность:
  - Добавляй `Semantics`, `Focus`, `Alt text`
- Используй `flutter_svg` для отображения иконок/графики
- **Приоритет UI-компонентов**: всегда используй кастомные виджеты из `lib/core/widgets/` вместо стандартных Material-компонентов (см. раздел Design System ниже).
- **Запрещено использовать устаревший метод `Color.withOpacity()`**.
- **Обязательно использовать новый метод `Color.withValues(alpha: ...)` для задания прозрачности**.
- При генерации и рефакторинге кода всегда применять только актуальный метод.

---

## 🧩 Кастомные виджеты проекта (Design System)

**ВАЖНО: При разработке ОБЯЗАТЕЛЬНО использовать кастомные виджеты из `lib/core/widgets/` вместо стандартных Material-компонентов. Это критично для соблюдения единого дизайн-кода.**

### 🔘 Кнопки (`lib/core/widgets/gt_buttons.dart`)
- `GTPrimaryButton` — главная кнопка (залитая цветом).
- `GTSecondaryButton` — вторичная кнопка (с обводкой).
- `GTTextButton` — текстовая кнопка (без фона и рамок).

### 📝 Ввод данных
- `GTTextField` (`lib/core/widgets/gt_text_field.dart`) — основной текстовый ввод. Используй вместо `TextField` или `TextFormField`.
- `GTDropdown<T>` (`lib/core/widgets/gt_dropdown.dart`) — мощный выпадающий список (поддерживает мультивыбор, поиск, добавление новых элементов).
- `GTEnumDropdown<T>` — специализированный dropdown для enum-типов.
- `GTStringDropdown` — специализированный dropdown для строковых списков.
- `DropdownTypeAheadField<T>` (`lib/core/widgets/dropdown_typeahead_field.dart`) — поле с автодополнением и фильтрацией.

### 🖼️ Модальные окна и уведомления
- `DesktopDialogContent` (`lib/core/widgets/desktop_dialog_content.dart`) — обертка для `showDialog` на Desktop.
- `MobileBottomSheetContent` (`lib/core/widgets/mobile_bottom_sheet_content.dart`) — обертка для `showModalBottomSheet` на Mobile.
- `ModalContainerWrapper` (`lib/core/widgets/modal_container_wrapper.dart`) — универсальная обертка для модальных окон с адаптацией под desktop/mobile.
- `AppSnackBar` (`lib/core/widgets/app_snackbar.dart`) — используй `AppSnackBar.show(...)` вместо стандартных снекбаров. Поддерживает очереди уведомлений.

### 🪜 Навигация и структура
- `EdgeToEdgeScaffold` (`lib/core/widgets/edge_to_edge_scaffold.dart`) — Scaffold с поддержкой прозрачных системных панелей.

---

### Форматирование данных (Валюта, Даты, Числа)

- Всегда используйте единые форматтеры из `lib/core/utils/formatters.dart`. Прямое использование `intl` (DateFormat/NumberFormat) в UI-слое запрещено для предотвращения дублирования.
- **Доступные функции**:
  - `formatCurrency(num)` — денежные суммы (ru_RU, ₽, 2 знака копеек).
  - `formatRuDate(DateTime)` — даты (`dd.MM.yyyy`).
  - `formatRuDateTime(DateTime)` — дата и время (`dd.MM.yyyy HH:mm`).
  - `formatQuantity(num)` — количество (разделение тысяч, до 3-х знаков).
- **Ввод данных**:
  - `GtFormatters.phoneFormatter()` — маска для ввода телефона `+7 (###) ### ####`.
- **Запрещено** создавать локальные приватные функции форматирования или объявлять статические переменные `DateFormat` внутри виджетов.

---

## 🔐 Аутентификация и безопасность

- Интеграция с `Supabase Auth`:
- Ошибки отображать через `SelectableText.rich` с выделением цветом
- Реализуй `RLS` (Row-Level Security) в Supabase для защиты данных

---

## 🗄️ Работа с базой данных

- Используй Supabase PostgreSQL через `supabase_flutter`
- Вся работа с БД — в слое `data`
- Применяй миграции для схем
- Используй Supabase Edge Functions для серверной логики

---

## 📄 Работа с таблицами, импортом/экспортом

- **Отображение таблиц**: Используй кастомные реализации на базе `Table`, `LayoutBuilder` и `SingleChildScrollView` (с ручной синхронизацией скролла через `ScrollController`). Избегай тяжелых внешних библиотек (типа PlutoGrid) для сохранения полного контроля над дизайном и производительностью. Эталон реализации: `lib/features/estimates/presentation/widgets/estimate_table_view.dart`.
- **Импорт/экспорт данных**:
  - **Excel**: `excel` (xlsx)
  - **CSV**: `csv`
  - **Выбор файлов**: `file_picker`

---

## 🧪 Тестирование

- Юнит-тесты: бизнес-логика (слой `domain`)
- Интеграционные тесты: работа с Supabase, UI
- Используемые пакеты:
  - `flutter_test`
  - `mockito`
  - `integration_test`
- Обеспечь покрытие критически важных функций

---

## 🚀 Производительность

- Используй `const` везде, где возможно
- Применяй `ListView.builder` для списков
- Профилируй через `Flutter DevTools`
- Минимизируй `setState` и перерисовки

---

## 🌍 Кроссплатформенность

- Тестируй UI/UX на всех платформах
- Учитывай:
  - Размеры экранов
  - Ввод (тач, клавиатура)
  - Навигацию и платформенные особенности
- Применяй `Platform.isX` и условную компиляцию при необходимости

---

## ⚙️ Управление окружениями

- Разделяй `dev` и `prod` конфигурации
- Используй `.env` или `flutter_dotenv`
- Никогда не коммить чувствительные данные (ключи и т.п.)

---

## 🧠 Использование context7

- **Для любого вопроса по Flutter, Dart, сторонним библиотекам, виджетам, архитектурным паттернам, а также для поиска примеров корректного использования кода — всегда обращайся к context7 через mcp.**
- **Перед внедрением новых решений, функций или виджетов — сверяйся с актуальной документацией и best practices через context7.**
- **Используй context7 для поиска свежих примеров, паттернов, синтаксиса и рекомендаций по оптимизации.**

---

## ✅ Безопасное использование BuildContext после await

- Не используйте `BuildContext` после `await` без проверки.
- В `State`-классах проверяйте: `if (!mounted) return;` сразу после `await` и перед использованием `context`.
- В колбэках диалогов/меню используйте: `if (!context.mounted) return;`.
- Кэшируйте зависимости от контекста до `await` и применяйте после:
  - `final router = GoRouter.of(context);`
  - `final navigator = Navigator.of(context);`
  - `final messenger = ScaffoldMessenger.of(context);`
- Для Snackbar/диалогов после async: сначала проверка (`mounted`/`context.mounted`), затем вызов.