---
description: 
globs: 
alwaysApply: true
---
---
description: Правила для Flutter-приложения с использованием Supabase и актуализацией знаний через context7
globs:
  - lib/**/*.dart
  - test/**/*.dart
---

# 📱 Общие требования

- **Целевые платформы**: iOS, Android, WebR
- **Фреймворк**: Flutter
- **Бэкенд**: Supabase
- **Стиль**: строгий черно-белый минимализм
- **Темы**: поддержка светлой и тёмной темы
- **Удаляй неиспользуемые переменные, функции и виджеты, которые появляются в ходе рефакторинга или доработки кода.**
- **Для получения актуальной документации, best practices, примеров использования функций, виджетов, библиотек Flutter и сторонних пакетов — обязательно обращайся к context7 через mcp. Используй context7 для поиска свежих решений, паттернов и корректного синтаксиса.**

---

## 🧱 Архитектура и структура проекта

- Придерживайся **Clean Architecture** с разделением на слои:
  - `presentation/` — UI
  - `domain/` — бизнес-логика
  - `data/` — работа с API/БД
- Организуй код по фичам: `lib/features/<feature_name>/`
- Используй `Riverpod` для управления состоянием и внедрения зависимостей
- Применяй `Freezed` для создания неизменяемых и sealed-классов
- Используй `json_serializable` для сериализации/десериализации моделей

---

## 🎨 UI и UX

- Используй `go_router` для маршрутизации
- Создавай **адаптивные** и **переиспользуемые** виджеты
- Всегда используй `const`-конструкторы, где это возможно
- Применяй `LayoutBuilder`, `MediaQuery`, `ThemeData` для адаптивности
- Учитывай доступность:
  - Добавляй `Semantics`, `Focus`, `Alt text`
- Используй `flutter_svg` для отображения иконок/графики
- Состояния экранов лучше управлять через `flutter_hooks` (опционально)
- **Запрещено использовать устаревший метод `Color.withOpacity()`**.
- **Обязательно использовать новый метод `Color.withValues(alpha: ...)` для задания прозрачности**.
- При генерации и рефакторинге кода всегда применять только актуальный метод.

### Форматирование сумм и дат

- Всегда используйте единые форматтеры из `lib/core/utils/formatters.dart`:
  - `formatCurrency(num)` для денежных сумм (ru_RU, ₽, 2 знака копеек)
  - `formatRuDate(DateTime)` для дат (`dd.MM.yyyy`, ru_RU)
- Запрещено дублировать локальные приватные `_formatCurrency/_formatDate` в виджетах/экранах.

---

## 🔐 Аутентификация и безопасность

- Интеграция с `Supabase Auth`:
- Ошибки отображать через `SelectableText.rich` с выделением цветом
- Реализуй `RLS` (Row-Level Security) в Supabase для защиты данных

---

## 🗄️ Работа с базой данных

- Используй Supabase PostgreSQL через `supabase_flutter`
- Вся работа с БД — в слое `data`
- Применяй миграции для схем
- Используй Supabase Edge Functions для серверной логики

---

## 📄 Работа с таблицами, импортом/экспортом

- Отображение таблиц: `PlutoGrid` (MIT, мощный функционал)
- Импорт/экспорт данных:
  - **Excel**: `excel` (xlsx)
  - **CSV**: `csv`
  - **Выбор файлов**: `file_picker`

---

## 🧪 Тестирование

- Юнит-тесты: бизнес-логика (слой `domain`)
- Интеграционные тесты: работа с Supabase, UI
- Используемые пакеты:
  - `flutter_test`
  - `mockito`
  - `integration_test`
- Обеспечь покрытие критически важных функций

---

## 🚀 Производительность

- Используй `const` везде, где возможно
- Применяй `ListView.builder` для списков
- Профилируй через `Flutter DevTools`
- Минимизируй `setState` и перерисовки

---

## 🌍 Кроссплатформенность

- Поддержка iOS, Android, Web
- Тестируй UI/UX на всех платформах
- Учитывай:
  - Размеры экранов
  - Ввод (тач, клавиатура)
  - Навигацию и платформенные особенности
- Применяй `Platform.isX` и условную компиляцию при необходимости

---

## ⚙️ Управление окружениями

- Разделяй `dev` и `prod` конфигурации
- Используй `.env` или `flutter_dotenv`
- Никогда не коммить чувствительные данные (ключи и т.п.)

---

## 🧠 Использование context7

- **Для любого вопроса по Flutter, Dart, сторонним библиотекам, виджетам, архитектурным паттернам, а также для поиска примеров корректного использования кода — всегда обращайся к context7 через mcp.**
- **Перед внедрением новых решений, функций или виджетов — сверяйся с актуальной документацией и best practices через context7.**
- **Используй context7 для поиска свежих примеров, паттернов, синтаксиса и рекомендаций по оптимизации.**

---

## ✅ Безопасное использование BuildContext после await

- Не используйте `BuildContext` после `await` без проверки.
- В `State`-классах проверяйте: `if (!mounted) return;` сразу после `await` и перед использованием `context`.
- В колбэках диалогов/меню используйте: `if (!context.mounted) return;`.
- Кэшируйте зависимости от контекста до `await` и применяйте после:
  - `final router = GoRouter.of(context);`
  - `final navigator = Navigator.of(context);`
  - `final messenger = ScaffoldMessenger.of(context);`
- Для Snackbar/диалогов после async: сначала проверка (`mounted`/`context.mounted`), затем вызов.